syntax = "proto3";

//package NetworkedPhysics;

// A tcp request that does a random database action
message Snapshot {
    message Vector3 {
        float x = 1;
        float y = 2;
        float z = 3;
    }
    
    bool interacting = 1;
    Vector3 position = 2;
}